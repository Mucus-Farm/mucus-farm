[profile.default]
src = 'src'
out = 'out'
libs = ['lib']

[rpc_endpoints]
mainnet = '${MAINNET_RPC_URL}'
goerli = '${GOERLI_RPC_URL}'

# gas_reports = ['Mucus', 'DividendsPairStaking', 'FrogsAndDogs', 'MucusFarm']

# See more config options https://github.com/foundry-rs/foundry/tree/master/config